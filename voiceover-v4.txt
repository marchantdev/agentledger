A-I agents now approve payments and execute trades autonomously. But when something goes wrong, there's no proof of what the agent actually decided.

Agent Ledger. Every agent decision gets a tamper-proof receipt on Casper. If an agent gets paid, it leaves a receipt.

Open the Agent Workbench. Pick a scenario — here, a vendor payment. Watch the treasury agent work. It reads the invoice, checks the vendor's trust score, evaluates the budget threshold, and decides: approve, ten thousand dollars. Then it records the decision hash on Casper testnet. Every step is visible. Nothing is hidden.

Here's the receipt. Every field is on-chain: the agent, the action, and the job payment reference hash that links this decision to the work the agent was paid for. Open the raw Casper proof — transaction hash, block height, and the exact named arguments stored in the smart contract. Verification runs client-side against Casper R-P-C.

Now watch. Click try tampering. The payment amount changes from ten thousand to fifteen thousand. The system re-hashes and checks against the chain. Instantly: tampered. The hash mismatch proves exactly what the agent originally decided. The ledger never lies.

Real scenario: a vendor claims fifteen thousand dollars. Open the dispute case file. The on-chain receipt proves the agent approved ten thousand, not fifteen. Hash mismatch. Claim disproved by cryptographic evidence. Resolved in seconds, not weeks.

Agent Ledger. Verifiable receipts for the agent economy. Built on Casper. Open source on GitHub.